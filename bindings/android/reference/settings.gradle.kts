// The Kotlin head's reference: its own Gradle build, like the conformance
// run, so that documenting the API needs no Android SDK. Run with the
// wrapper one directory up.
rootProject.name = "tacenta-reference"
